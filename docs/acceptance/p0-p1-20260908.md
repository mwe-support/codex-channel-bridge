# P0 / P1 execution — 2026-09-08

Historical starting status: in progress. The user authorized the roadmap's persistent-storage work
and acceptance closeout. This record does not change a release tag or mark an
individual requirement complete.

Current scope, 2026-09-10: P0 storage and P1 mainline feature acceptance are
complete. Windows remains deferred to its independent branch; release
preparation/publication remains separate. See the [requirements ledger](../feature-requirements.md)
and [final provider-boundary verdict](c2c-boundaries-20260910.md#cleanup-and-verdict).
The dated checkpoints below retain their original observations.

## P0: persistent test-deployment storage

At the start of P0, the active macOS test Profile and the disabled secondary
Profile kept Bridge state, Codex home and Workspace below temporary directories. The active
Profile's WhatsApp credentials remained present after its active-generation
reference disappeared. Reference recovery, existing-account reconnection and
two real group-message round trips succeeded without pairing again.

Execution gates:

1. Prepare stopped-Profile backup manifests and verified, owner-only operator
   snapshots, including Codex home, Workspace and external credentials.
2. Validate an operator-managed move to persistent backing directories while
   preserving the original configured Codex home and Workspace paths. Do not
   rewrite native history or claim general arbitrary-path restore support.
3. Preserve Profile IDs, account ownership, authentication, Thread bindings and
   Bridge records. Keep an explicit rollback path.
4. Validate the restored manifests, restart and verify native Thread access and
   real QQ / WhatsApp delivery before declaring the migration successful.
5. Document any required compatibility-path startup checks and prove the known
   temporary-file cleaner cannot traverse the persistent authentication data.

### Verified macOS result

P0 storage cutover passed. Both roots now have owner-only persistent backing
directories outside the temporary tree. The old root names are host-local
symbolic aliases, preserving the configured native Codex home and Workspace
paths. Bridge state and Secret files use their persistent paths directly. No
native history file was rewritten, no Profile/account was reassigned and no
WhatsApp pairing was repeated.

- Operator snapshots were extracted and compared with their stopped sources:
  7,215 files / 149,856,180 bytes for the active Profile, and 1,373 files /
  66,059,834 bytes for the disabled Profile. Restore validation passed before
  and after the backing-directory move.
- The original four active-Profile bindings and one disabled-Profile binding
  were preserved. All four active native Threads remained readable through
  the model-settings control operation. Native login and the original WhatsApp
  identity were verified without printing credentials or provider identifiers.
- A stopped-Supervisor recovery drill removed only the verified aliases and
  recreated them through the persistent operator startup entry. An intentionally
  conflicting test file was rejected without overwrite. The startup entry
  imports the existing CLI in the same process; it is not another daemon.
- The known macOS temporary cleaner does not follow symbolic aliases. A
  non-following traversal of each compatibility alias visited zero files.
  Persistent data therefore lies outside that cleaner's traversal. The startup
  entry recreates missing aliases after temporary-directory loss.
- A new QQ private input and a new WhatsApp private input each completed,
  received an accepted provider result and produced the exact expected reply
  in the desktop client. WhatsApp's private typing indicator was observed
  during work and absent after completion.
- The active Profile and both its adapters returned to `ready`; the secondary
  Profile remained disabled. Body-free relocation Audit Records were appended.

The resulting Configuration Revision is
`d7cef250bd37da8ddcf3f7267a546d609941e518d2a8dacc6cb70ed1b21b4bcc`.
This is a deployment-specific, same-host filesystem procedure, not a new
cross-platform or arbitrary-native-path restore feature. The test Supervisor
still uses its operator-controlled foreground startup; automatic OS service
registration was not added as part of this storage operation.

## P1: acceptance closeout

| Scope | Starting point | Required next evidence |
| --- | --- | --- |
| FR-013 / FR-011 / FR-012 | macOS, Linux and Docker evidence exists | Reconcile mainline evidence; unattended Windows proceeds separately on `codex/windows-unattended` |
| FR-010 | macOS downloads and digests verified | Native Linux and Docker attachment delivery and receiving-client verification; Windows remains on its branch |
| FR-006 | QQ C2C streaming has real positive evidence | Remaining expiry, rate-limit, interruption, restart, ambiguity and concurrency scenarios |
| FR-001 / FR-003 | Real positive WhatsApp replies and concurrency evidence exist | Outstanding typing visibility/cleanup and independent-control scenarios from each requirement's checklist |
| Release | Current work is Next on the CLI candidate branch | Exact-source checks, bilingual records and applicable upgrade/rollback gates before considering another immutable RC |

The existing Windows task has been asked to reconcile its retained results and
current permissions without modifying WIP, credentials, global ACLs or the host
Codex installation. The user subsequently authorized the isolated SCM test and
confirmed availability for local elevated-terminal and hidden-password input.
Only the named test service is in scope; no service-account right is presumed.

The task prioritizes closing documented gaps; an environment refusal or a
successful compilation is not a replacement for a real lifecycle test.

### Historical Windows checkpoint (2026-09-08)

The content-free [Windows report](https://github.com/mwe-support/codex-channel-bridge/blob/32db5d6641a847612841ddb9deb9cf4ec2544e66/acceptance/windows-p1-status-20260908.json)
identifies candidate `be9e4c0` and tested runtime `3348e3d`. Its complete-check
baseline is still `b642054`: 252 passed, one failed and four skipped. Incremental
results report two service tests, five control contracts and ten expected CLI
outcomes passed; the real file-symlink check failed.

SCM creation and file-symlink permissions are unavailable in the original
non-elevated session, and the current native SCM lifecycle remains unverified.
The isolated operator script has been prepared, reviewed and dispatched to the
Windows local execution flow after user authorization. No permission assertion
is removed or replaced to make this gate appear to pass.


### Additional real macOS evidence

- QQ group and private work used distinct Threads and overlapped for 21,428 ms.
  The private native stream reached `done` with `nextIndex: 26` and a 1,407-character
  prefix while the group remained active. Both final results were accepted on
  attempt one; the group had no answer-stream record.
- A subsequent QQ private Turn was interrupted after 13,204 ms. Its partial
  stream reached `fallback`; the terminal Outbox result was accepted on attempt
  one. Earlier attempts that completed before interruption are not counted as
  interruption acceptance. This run does not prove simultaneous interruption
  isolation between two active conversations.
- WhatsApp private typing visibility and completion cleanup passed in P0.
  The real group follow-up also passed: a native participant mention started
  the Turn, the desktop displayed the bot typing, and the exact final reply
  arrived with typing absent afterward. The Turn interval was 40,216 ms and
  the final delivery was accepted on attempt one. This verifies visible
  presence and final cleanup, not continuous visual presence throughout every
  refresh interval or concurrent-turn interruption isolation.
- The post-relocation QQ file check completed. The generated UTF-8 file is
  29 bytes, matches the expected content, and has the same SHA-256 as the durable
  snapshot and attachment Outbox metadata:
  `9ace8531590041d98c499e8fd47c41d26944838ad72210d424df4488d3cd1b77`.
  The attachment was accepted on attempt one; the user subsequently confirmed
  receipt in QQ. This additional run does not claim verification of downloaded
  client bytes. The user sent the test manually after desktop window reading
  was rejected by automatic approval review.
- The user selected the disabled second QQ Bot for sequential cross-platform
  tests and separate target-host WhatsApp pairing, preserving the Mac login.
  Target credentials, native login and pairing must be provisioned through their
  supported local flows before claiming real target-host delivery. Existing
  Profile authentication and history must not be copied automatically.

At the P0 milestone the active macOS Profile and both adapters were ready; the
second Profile remained stopped. The executable reported Codex 0.153.4 with capability-compatible
but behaviorally `unverified` status. These checks do not promote it to a verified
compatibility snapshot or close the remaining provider-fault/release gates.


### Mainline continuation (2026-09-09)

Unattended Windows development is now separate. The current mainline regression,
service/container lifecycle and real Channel results are recorded in
[Mainline acceptance](mainline-20260909.md).

A separately paired WhatsApp account was added on macOS with one allowed test
group and private chats denied. Its identity differs from the retained original
account; its real group request/reply passed. The current QQ and new WhatsApp
adapters are ready. The retained original WhatsApp adapter is degraded after an
unsuccessful reconnect, so the Profile's aggregate state is degraded. No new
login or destructive cleanup was applied to that original account. This does
not invalidate the recorded earlier P0 checkpoint.

Later [boundary acceptance](mainline-closeout-20260909.md) allowed the fresh test
participant in the new account's private chat and verified independent
private/group interruption. The stale original adapter was then reversibly
disabled via canonical CLI configuration apply, preserving authentication,
Archive and bindings. The active Profile and its enabled QQ/WhatsApp adapters
now report ready; the old account was not logged out or reauthenticated.
