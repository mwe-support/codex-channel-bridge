# 统一 CLI 验收 — 2026-09-08

需求：FR-013。运行时代码候选：
[`3348e3de0990d0be26d192fe22bab05a03deb4c1`](https://github.com/mwe-support/codex-channel-bridge/commit/3348e3de0990d0be26d192fe22bab05a03deb4c1)。
Git tree：`f10009db7deada5cfde9ab74b9c232ca349efe5e`。
这是尚未发布的 Next 工作，不是新发行版，也不代表 Windows SCM 门槛已通过。

## 已确认结果

| 目标／操作 | 结果 |
| --- | --- |
| macOS `npm run check` | 退出 0：258 项单元测试、4 项发布／安装器测试、4 项平台检查，无失败和跳过 |
| Unix PTY quick/full setup | 配置写入并校验通过；两项合成密钥不回显，文件权限为 owner-only |
| PTY 取消 | 取消初始化不写文件；取消服务注册保留配置，也不会提示启动另一个服务 |
| macOS 交互式 `config edit` | 真实 PTY 中验证外部编辑器、校验预览、确认与原子保存 |
| macOS 配置 | CLI 设置保存／应用、Profile 停用／重新启用、停用时拒绝原生模型操作，以及恢复原 Configuration Revision 均通过 |
| macOS 服务 | 预览、错误确认、重复注册拒绝、注册后不立即启动、启动、重启、停止、卸载和保留配置／SQLite 状态均通过 |
| Dashboard | 真实 CLI `--open`、loopback HTML、带 capability 的状态查询、无 capability 路径拒绝、停止后 capability 失效均通过 |
| 原生模型，2026-09-05 | 动态目录、原生默认值写入／回读、Thread 模型／推理强度更新，以及使用更新设置的 QQ 回复通过；原绑定和隔离原生默认值已恢复 |
| Linux，`marvel-mini-pc` | 258 项单元测试、4 个 PTY 场景、5 项真实 IPC 契约、原生协议和 Supervisor 隔离契约通过 |
| Linux systemd 用户服务 | 安装／启动／重启／模型查询／停止／卸载通过，配置文件名包含空格、`$` 和 `%`；配置和数据保留 |
| Linux Docker，`marvel-mini-pc` | 生产 Dockerfile 通过显式 `CODEX_VERSION=0.153.4` 构建；非 root 容器启动、原生模型查询、配置应用、重启和退出码 0 的停止通过 |

macOS、Linux 原生证据使用管理员提供的 Codex `0.153.4`，稳定 Schema 摘要为：
`d3eace08be5dca386bfd1f1e8df650058b4113f1e10870a284d775d75517576a`。
这是实测事实，不是版本白名单。运行时仍可能显示 `unverified`；就绪状态和协议协商
不等于账号能够完成需要认证的模型请求。

Linux 安装器先正确拒绝了已有用户 unit 目录的 `0775` 权限。验收确认该组没有其他
用户，临时移除组写权限，执行隔离服务生命周期后恢复 `0775`；安装器没有放宽安全
检查。测试服务注册、元数据与 Docker 容器已移除，测试数据和结果保留。未安装或
升级任何主机 Codex。

## 回归检查与实现修正

- 配置测试拒绝无效目标、无效／过期编辑、错误确认、保留键，以及通过 CLI 参数传值
  的密钥输入。
- 原生模型测试覆盖作用域、动态发现、可选能力、用户配置层并发版本与输出脱敏；
  查询不执行 Thread resume。
- QQ 生命周期测试保留其他适配器和 Codex，拒绝待投递状态，并拒绝静默检查期间及
  断开后到达的工作输入，同时保留归档记录，不建立故障积压。
- 真实 PTY 暴露并验证修复了重复 readline 监听器导致的 setup 密钥回显。
  Linux 提示符解析现已处理 ANSI 光标序列。
- 服务验收暴露了 Codex 启动脚本需要明确 Node PATH，以及原生注销后仍应等待
  Supervisor 退出这两个问题，均已修正。
- Windows 在父进程完成 Job Object 关联后才允许启动 Profile 子进程；父进程提前
  停止不能触发工作。
- systemd 参数和环境变量分别保留其不同的 `$` 处理规则。

## 历史 Windows 证据与剩余门槛

较早 CLI 候选 `b642054c632d28e8745d7c7f38cb2b5c27a42b44` 已在独立 Windows
worktree 验收。报告记录了原生适配器编译／参数检查、6 项定向测试、76 项脚本式 CLI
检查，以及真实 TTY setup／secret 测试通过。完整检查为 252 通过、1 失败、4 项既有
平台跳过；失败是缺少主机权限时创建真实文件符号链接。未删除或替换断言来获得通过。

下一提交修正 systemd 渲染并增加测试。恢复后的 Windows 运行仍需核对最终报告，才能
声明新的完整 Windows 结果。普通用户服务权限拒绝不等于 SCM 生命周期验收。
真实 SCM 注册／启动／停止／卸载仍要求所选账号的原生权限和服务登录前提。
未使用 UAC、服务登录权限授予、全局 ACL 放宽、账号替换或计划任务回退。
原 Windows WIP、stash 和旧证据均保留。

## 最新真实 QQ 边界

2026-09-08 已把候选加载到现有 macOS 测试部署。真实 QQ 输入获准进入工作；活动工作
期间，CLI 断开操作正确以 `channel_account_busy` 拒绝。原生 Turn 失败后，失败状态
已投递到 QQ。原生检查将原因归为认证／授权问题；选定 Codex home 的状态为
`Not logged in`。

用户授权复用本机已有钥匙串登录，并完成系统授权。仅读取精确匹配源 home 的 Codex
凭证条目，写入测试 Profile 的 owner-only 原生认证文件；未打印凭证，也未改写源凭证值。
随后原生登录状态检查通过。重新加载测试 Supervisor 后，新的真实 QQ 输入收到了准确的
预期正向回复；最后一轮测试没有改变 Thread 绑定。

失败 Turn 结束后，QQ 断开／重连也通过。在 CLI 检查点，WhatsApp 适配器为 degraded，
无法加载活跃认证；QQ 仍能独立工作。后续检查确认丢失的是活跃认证代际引用，原凭据
仍然存在。用户授权恢复引用后，原账号已恢复连接，无需重新配对。随后的持久存储迁移
及新一轮 QQ / WhatsApp 收发见 [P0 / P1 执行记录](p0-p1-20260908.md)。

无人值守 Windows 后续已移到 `codex/windows-unattended`。更新后的 macOS/Linux/
Docker 结果见 [2026-09-09 主线验收](mainline-20260909.md)。适用的剩余门槛关闭前，
需求保持 `awaiting-acceptance`。
