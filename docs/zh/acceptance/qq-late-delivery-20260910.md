# QQ 延迟投递与并发中断 — 2026-09-10

接续[主线边界验收](mainline-closeout-20260909.md)。操作者已开启测试群的机器人
主动发言权限。原生 Linux 从保留的 `9ea63a0` 快照启动，复用原 Profile、认证及
绑定；两个适配器恢复 ready，没有更改凭据、宿主 Codex 安装或运行源码。

## 过期锚点回退

通过生产适配器和固定 SDK 的 REST 探测复用此前失败的同一个旧群消息锚点。
被动请求再次返回 HTTP 400 / 业务码 `40034005`，随后自动主动回退获接受，
QQ 群内也在正确 Bot 名下独立观察到准确探测回复。此前权限拒绝的结果另行保留。

这证明了真实的过期锚点到主动发送分支。探测使用最小 ready 门面，没有创建第二条
Gateway 连接，本身不作为 Codex Turn 验收。

## 真实六分钟群任务

一条新的 QQ 群提及请求通过正常 Codex Thread 执行前台 360 秒等待，Turn 在
372,419 毫秒后完成。唯一终态 Outbox 首次尝试即获接受并有平台回执，QQ 群客户端
显示的完整文字与已提交结果一致。群聊没有答案流记录。它在下述私聊中断后
210,352 毫秒完成，确认群任务持续独立执行。

## 群任务期间的私聊原生流

同一 QQ Channel Account 在不同原生 Thread 收到独立私聊请求。群任务执行期间，
私聊原生流收到生成帧，接收客户端可见编号正文持续增长。随后在该私聊发送 `/stop`，
紧接着读取状态，确认私聊 `interrupted`、群聊仍为 `started`。

私聊 Turn 耗时 29,103 毫秒，中断时已有 30 个被接受生成帧、3,905 字符持久前缀。
其状态进入 `fallback`；中断通知以回复序号 2 首次 Outbox 投递获接受，且在私聊
客户端可见。这证明中断和接收会话隔离，不宣称被中断流收到了成功 DONE。

## 范围及保留门槛

- REST 成功回退与正常群任务是两份独立证据，不从群任务 Outbox 行推断其实际
  提供商 HTTP 分支。
- 本项属于原生 Linux 平台验收，不是新一轮 Docker 或 Windows 测试。
- 真实 C2C 过期拒绝、提供商限流拒绝及其他尚未观察的流式/连接边界继续保留；
  FR-006 仍为 `awaiting-acceptance`。
- 历史失败投递未改写，也未静默重放；没有更改发行标签、项目版本或已发布产物。

验收后没有活动输入、待决审批或待投递 Outbox，SQLite `quick_check` 返回 `ok`。
原生 Linux 完成有界排空并零退出码停止；持久配置、认证、历史及启动脚本保留，
测试运行时已停止。
