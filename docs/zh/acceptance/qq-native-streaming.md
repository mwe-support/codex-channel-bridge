# QQ 原生流式——Next 工作区验证

日期：2026-09-04。状态：真实平台主链路已通过，边界验收待补。本修复没有提交或发布 tag；保留既有无关工作区改动。

## 本机已验证

- 最终候选通过 `npm run build`、全部 236 项单元测试、中英文 `npm run docs:build`
  及 `git diff --check`。
- 修复前通过 `codex-event-router.test` 复现：终态前没有任何答案更新。
  修复后仅投射对应 Turn 的 `final_answer` item 增量，排除旧 Turn 与 commentary。
- 单元测试覆盖 QQ 精确请求体（单锚点、稳定序号/身份、递增 index、生成/DONE 状态）、
  拒绝群聊流帧、合并更新、持久序号分配、不确定发送恢复、前缀冲突回退、DONE 去重、
  Unicode 无损分段及 schema 迁移。
- `npm run test:contract` 在原生 macOS 通过，使用管理员提供的 Codex 0.149.1，
  稳定 schema SHA-256 为
  `9b3de71a5a2ffc980b792a18aa8f8dec3f85f48829560222a0264fe494b679a9`。
  沙箱内尝试在初始化前退出，宿主环境复测通过。
- 一次真实 Codex Turn 在提示中禁止工具，正常结束前实际产生 293 次答案增量，
  路由收到 293 次更新，终态文本长度 1,663 字符。只保留计数、phase 和状态，不保留正文。
- 发布工具测试 4/4；平台文件契约测试 4/4。它们不代表本次修改在 Linux、Docker 或 Windows 执行过。

## 部署与真实 QQ 证据

测试 Profile 已有界排空；本地快照包含状态、Codex home、Workspace 与外部凭证，
共校验 2,656 个文件、84,493,413 字节。backup-finish 审计记录写入后，
新的停止状态快照也已与迁移源摘要核对一致。取得所要求的快照完成后操作员确认，
通过受支持的显式迁移完成 schema 9→10。重启后 Supervisor 报告 QQ、WhatsApp
均就绪，独立 Dashboard 始终保留运行。

首次真实私聊接受一个生成帧后，回退成普通完整答案。本地条件错误地将
`remain_msg_len=0` 当成可写容量耗尽。新增每帧成功都返回零值的回归测试，
移除错误条件前失败，修复后通过。再次真实测试前，已通过有界 Supervisor 重启部署修复。

| 原生 macOS QQ 客户端场景 | 无正文证据 |
| --- | --- |
| 私聊长答案 | Codex 仍为 `started` 时，7 个成功帧累计 281 字符，随后 23 帧累计 997 字符。同一客户端消息可见从第 2 段中途增长到第 9 段。最终 44 帧（含成功 DONE），1,862 字符，`completed`。 |
| 最终投递关联 | 一个成功 Outbox 分段，attempt 为 1；提供商回执 ID 与持久原生流 ID 相同。未观察到另发普通完整答案。 |
| 私聊短答案 | 2 帧（含 DONE）、11 字符、Outbox 首次投递成功、同一原生回执 ID。另一个 8 字符短回复也通过。 |
| 群聊完整答案 | 从真实成员选择器提及配置的测试 Bot；1,366 字符完整回复，Outbox 首次投递成功，没有答案流记录。 |

提供商在剩余长度为零期间仍接受续帧与 DONE。因此实现仅记录该字段，不把它作为
准入容量；不声称其未明确文档化的精确含义。思考或工具执行期间如果尚无
`final_answer` 增量，仍然没有可流式展示的答案正文。

仍需验证：长周期过期/限流/连接边界、中断/重启恢复，以及流式期间真实重叠的
私聊/群聊隔离。本次尝试群聊/私聊重叠时，群聊过早结束，无法建立同时活跃 Turn；
它只证明两条路由均成功，不代表并发验收。WhatsApp 已连接，但本修复尚未新增
WhatsApp 客户端交互。模拟边界测试不替代这些待补真实平台检查。
