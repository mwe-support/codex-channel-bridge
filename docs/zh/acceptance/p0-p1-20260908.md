# P0 / P1 执行记录 — 2026-09-08

初始检查点状态：进行中。用户已授权执行路线图中的持久存储和验收收口工作。本记录不改变
发行标签，也不单独把任何功能需求标记为完成。

2026-09-10 当前范围：P0 持久存储和 P1 主线功能验收已完成。Windows 延期到
独立分支，发行准备/发布仍单独执行。见[需求台账](../feature-requirements.md)及
[最终平台边界结论](c2c-boundaries-20260910.md#清理与结论)。下方各日期检查点
保留当时的观察记录。

## P0：测试部署持久存储

P0 开始时，macOS 活跃测试 Profile 与停用的次 Profile 都把 Bridge 状态、Codex home
和 Workspace 放在临时目录下。活跃 Profile 的 WhatsApp 活跃认证引用丢失时，
原凭据仍然存在。已恢复引用、重连原账号，并完成两次真实群聊收发，无需重新配对。

执行门槛：

1. 为停止的 Profile 生成备份清单，制作并校验 owner-only 的管理员快照，包含
   Codex home、Workspace 与外部凭据。
2. 验证管理员管理的持久存储迁移，同时保留原有配置中的 Codex home 与 Workspace
   路径。不改写原生历史，不宣称提供通用的任意路径恢复能力。
3. 保留 Profile ID、账号归属、认证、Thread 绑定与 Bridge 记录，并保留明确的回滚路径。
4. 校验恢复清单、重启、检查原生 Thread 访问和真实 QQ / WhatsApp 收发后，才能宣布
   迁移成功。
5. 记录必要的兼容路径启动检查，并验证已知的临时文件清理程序无法遍历持久认证数据。

### macOS 已验证结果

P0 存储切换通过。两个数据根已移入临时目录之外、仅所有者可访问的持久目录。
旧根目录名作为主机本地符号链接保留，使配置中的原生 Codex home 与 Workspace
路径不变；Bridge 状态与 Secret 文件直接使用持久路径。没有改写原生历史文件、
重新分配 Profile/账号或重新配对 WhatsApp。

- 管理员快照已解包并与停止的源数据逐项比较：活跃 Profile 为 7,215 个文件、
  149,856,180 字节；停用 Profile 为 1,373 个文件、66,059,834 字节。底层目录移动
  前后的恢复校验均通过。
- 保留了活跃 Profile 原有的 4 个绑定和停用 Profile 的 1 个绑定。4 个原生 Thread
  均可通过模型设置管理操作读取。已验证原生登录与原 WhatsApp 身份，未输出凭据
  或提供商标识。
- 停止 Supervisor 后的恢复演练只移除了已核对的路径别名，再由持久启动入口重建。
  人为创建的冲突测试文件被拒绝且未被覆盖。启动入口在同一进程导入现有 CLI，
  没有新增守护进程。
- 已知的 macOS 临时清理程序不跟随符号链接。对两个兼容入口执行不跟随链接的遍历，
  均访问到 0 个文件；持久数据因此不在该清理程序的遍历范围内。临时目录丢失后，
  启动入口会重建缺失的别名。
- 新的 QQ 私聊输入和 WhatsApp 私聊输入均完成，提供商结果为 accepted，桌面客户端
  收到准确的预期回复。WhatsApp 私聊的输入提示已观察到在执行中出现、完成后消失。
- 活跃 Profile 及其两个适配器恢复为 `ready`；次 Profile 继续保持停用。已追加
  不含正文的目录迁移 Audit Record。

最终 Configuration Revision 为
`d7cef250bd37da8ddcf3f7267a546d609941e518d2a8dacc6cb70ed1b21b4bcc`。
这是当前部署专用的同主机文件系统操作，不是新增跨平台或任意原生路径恢复能力。
测试 Supervisor 继续通过管理员控制的前台入口启动；本次存储操作没有新增 OS
服务自动注册。

## P1：验收收口

| 范围 | 起点 | 下一项必要证据 |
| --- | --- | --- |
| FR-013 / FR-011 / FR-012 | 已有 macOS、Linux、Docker 证据 | 核对主线证据；无人值守 Windows 在 `codex/windows-unattended` 单独推进 |
| FR-010 | macOS 下载与摘要已验证 | 原生 Linux、Docker 附件投递及接收端验证；Windows 保留在独立分支 |
| FR-006 | QQ C2C 流式已有真实正向证据 | 剩余的过期、限流、中断、重启、不确定结果与并发场景 |
| FR-001 / FR-003 | 已有真实 WhatsApp 正向回复与并发证据 | 各需求检查表中尚未完成的输入提示可见/清理与独立控制场景 |
| 发布 | 当前工作仍是 CLI 候选分支上的 Next | 考虑新不可变 RC 前完成准确源码检查、双语记录和适用的升级/回滚门槛 |

已请原 Windows 任务核对保留的结果与当前权限，不修改 WIP、凭据、全局 ACL
或宿主 Codex 安装。用户随后授权了隔离 SCM 测试，并确认可以配合本机提权终端
和无回显密码输入。仅具名测试服务在范围内，不假定服务登录权已经具备。

本轮优先关闭已记录的缺口；环境权限拒绝或编译成功不能代替真实生命周期验收。

### 历史 Windows 检查点（2026-09-08）

不含内容的 [Windows 报告](https://github.com/mwe-support/codex-channel-bridge/blob/32db5d6641a847612841ddb9deb9cf4ec2544e66/acceptance/windows-p1-status-20260908.json)
标明候选为 `be9e4c0`、实测运行时为 `3348e3d`。完整检查基线仍为 `b642054`：
252 通过、1 失败、4 跳过。增量结果记录了 2 项服务测试、5 项控制面契约和 10 项
CLI 预期结果通过；真实文件符号链接检查失败。

原非提权会话没有 SCM 创建和文件符号链接权限，新的原生 SCM 生命周期仍未验证。
隔离管理员脚本已准备并审阅，用户授权后已交由 Windows 本机执行流程。不会删除
或替换权限断言来使该门槛表面通过。


### 新增的 macOS 实测证据

- QQ 群聊和私聊使用不同 Thread，重叠执行 21,428 毫秒。私聊原生流到达
  `done`，`nextIndex: 26`，前缀长度 1,407 字符时，群聊仍在执行。两条最终结果
  均在首次投递获平台接受；群聊没有 answer-stream 记录。
- 后续一轮 QQ 私聊在 13,204 毫秒后成功中断，部分流进入 `fallback`，终态
  Outbox 首次投递获平台接受。此前在中断前已完成的尝试不计入中断验收；这一轮
  也不能证明两个同时活跃会话的中断隔离。
- P0 已验证 WhatsApp 私聊输入指示及完成后的清理。后续真实群聊验收也通过：
  从原生候选列表选中提及后启动 Turn，桌面显示机器人正在输入，随后收到准确的
  最终回复，回复后输入提示消失。Turn 时间段为 40,216 毫秒，最终投递首次获
  平台接受。这验证了提示可见和最终清理，不代表逐个刷新周期均完成视觉测量，
  也不替代并发 Turn 的中断隔离验收。
- 迁移后的 QQ 文件测试已完成：UTF-8 文件为 29 字节，内容符合预期，生成文件、
  持久快照及附件 Outbox 元数据的 SHA-256 相同：
  `9ace8531590041d98c499e8fd47c41d26944838ad72210d424df4488d3cd1b77`。
  附件首次投递获平台接受，用户随后确认 QQ 已收到。本次追加测试尚未校验接收端
  下载后的字节。桌面窗口读取遭自动审批拒绝后，由用户手动发送测试消息。
- 用户已选择停用的第二 QQ Bot 顺序进行跨平台测试，并在目标机单独配对
  WhatsApp、保留 Mac 登录。目标凭据、原生登录和配对须通过受支持的本机流程
  准备；不得自动复制已有 Profile 的认证与历史。
- 用户已授权 Windows 隔离 SCM 验收，并确认可在本机提权终端配合无回显密码
  输入。仅测试服务在授权范围内，不能假定服务登录权已经具备。

P0 检查点结束时，macOS 主 Profile 和两个渠道为 ready，第二 Profile 保持停止。
Codex 可执行文件报告 0.153.4，能力兼容但行为验证状态仍为 `unverified`。
本轮证据不将其提升为已验证快照，也不关闭剩余平台故障与发布门槛。

### 主线继续推进（2026-09-09）

无人值守 Windows 开发已独立。当前主线回归、服务/容器生命周期与真实渠道结果见
[主线验收](mainline-20260909.md)。

Mac 新增了单独配对的 WhatsApp 账号，仅开放一个测试群，私聊仍拒绝。新旧账号
身份不同，新账号真实群聊收发已通过。当前 QQ 和新 WhatsApp 适配器 ready；
保留的旧 WhatsApp 适配器重连未成功而处于 degraded，Profile 汇总状态因此也是
degraded。没有对旧账号重新登录或执行破坏性清理。这不会改写早先 P0 检查点的
记录。

后续[边界验收](mainline-closeout-20260909.md)允许了新账号私聊中的新测试参与者，
并验证私聊/群聊独立中断。随后通过规范 CLI 配置 apply 可逆禁用失效旧适配器，
保留认证、归档及绑定。当前活跃 Profile 和已启用 QQ/WhatsApp 适配器均报告 ready；
旧账号没有 logout，也没有重新认证。
